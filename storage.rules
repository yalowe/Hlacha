rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /pronunciations/{fileId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.role in ['Editor', 'Posek', 'SuperAdmin'];
    }
  }
}
