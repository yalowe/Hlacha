# Firebase Configuration
# Get these values from Firebase Console > Project Settings > General > Your apps > Web app

# Firebase API Key (public - safe to expose in client apps)
EXPO_PUBLIC_FIREBASE_API_KEY=your-api-key-here

# Firebase Auth Domain
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase Project ID
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase Messaging Sender ID
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012

# Firebase App ID
EXPO_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abc123def456

# Admin Authentication
# Set a secure random code for admin access (minimum 12 characters recommended)
EXPO_PUBLIC_ADMIN_SECRET_CODE=your-secure-admin-code-here

# ============================================================================
# SETUP INSTRUCTIONS:
# ============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Create Firebase Project:
#    - Go to https://console.firebase.google.com
#    - Click "Add project"
#    - Name: "Torah Learning App" or "למען שמו באהבה" (your choice)
#
# 3. Get Firebase Config:
#    - In Firebase Console, go to Project Settings
#    - Scroll to "Your apps" section
#    - Click "Web" icon (</>)
#    - Register app name: "Torah Learning Web"
#    - Copy the config values and paste above
#
# 4. Setup Firestore:
#    - In Firebase Console, go to "Firestore Database"
#    - Click "Create database"
#    - Start in "production mode"
#    - Choose location: europe-west (or closest to Israel)
#
# 5. Set Firestore Security Rules:
#    Go to Firestore > Rules and paste:
#
#    rules_version = '2';
#    service cloud.firestore {
#      match /databases/{database}/documents {
#        match /questions/{questionId} {
#          allow read: if true;
#          allow create: if request.auth != null;
#          allow update: if request.auth != null;
#          allow delete: if request.auth.uid == resource.data.askedBy;
#        }
#        match /questions/{questionId}/answers/{answerId} {
#          allow read: if true;
#          allow write: if request.auth != null;
#        }
#      }
#    }
#
# 6. Enable Authentication (Optional):
#    - Go to Authentication > Get started
#    - Enable "Anonymous" provider
#
# 7. Set Admin Code:
#    - Generate a secure random string (at least 12 characters)
#    - Save it securely - you'll need it to access admin features
#
# 8. Restart your development server after setting up .env
#
# ============================================================================
# SECURITY NOTES:
# ============================================================================
#
# - NEVER commit .env to git (it's in .gitignore)
# - Firebase API keys are safe to expose in client apps (they're public)
# - However, secure them with Firestore Security Rules
# - Admin code should be kept secret and shared only with admin users
# - For production, consider using Expo's EAS Secrets for sensitive values
#
